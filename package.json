{
  "name": "@fpworkshop/root",
  "version": "1.0.0",
  "description": "Main repo used for Skip's 'Functional Programming for front end developers' workshop",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean:modules": "rimraf ./**/node_modules && yarn install",
    "build": "lerna run --scope @fpworkshop/app  build --stream",
    "start": "lerna run --scope @fpworkshop/app start --stream"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "4.0.0",
    "rimraf": "3.0.2",
    "typescript": "4.5.2"
  }
}
